const profileCreateInput = require('../generated/prisma-client');
const { createProfile } = require('./Mutation.js');

it('Calls prisma', async () => {
  const fakePrismaCreateProfile  
  await createProfile();
});
